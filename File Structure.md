python --version
```
This confirms Python is installed. You should see something like `Python 3.10.x` or higher. If you get an error, Python isn't installed or isn't in your PATH — tell me and we'll fix it.

---

## Step 2: Understand the Folder Structure We're Building

Before writing any code, here's what Module 1's folder structure will look like when done:
```
PLIS/
├── README.md
├── data/
│   └── concepts.json        ← where your logged concepts get saved
└── tracker/
    └── log_concept.py       ← the Python script you'll run